//1. Declare uma variável de nome weight
let weight;

//2. Que tipo de dado é a variável aceima ?.
console.log(typeof weight)

//3. Declare uma variável e atribua valores para cada um dos dados:
    let name = Ryan
    let age = 19
    let stars = 29.2
    let isSubscribed = true
//
    //4. A Variavel student abaixo é de que tipo de dado?

    // 4.1 Atribua a ela as mesmas propriedades e valores do exercicio 3.

    //4.2 Mostre no console a seguinte mensagem:
     
        //<Name> de idade <age> e pesa <weight> kg.
        //atenção, substitua <name> <age> e <weight> pelos
        //valores de cada propriedade do objeto

        let = student
        console.log(typeof student)

        let student = {
            name: 'John',
            age: 30,
            weight: 88.6,
            isAdmin: true
        }

        console.log(`${student.name} tem ${student.age} anos e pesa 
        ${student.weight}`)

    
    //Declare uma variável do tipo Array, de nome
    //students e atribua a ela nenhum valor, ou seja,
    //somente o Array vazio.

    let students = []

    //6. Reatribua valor para a variável acima, colocando dentro dela o objeto
    //student da questão 4. ( nao copiar e colar o objeto, mas usar o objeto)
    //criando e inserir ele no array.

    student = [
        student
    ]

    console.log(student)

    //7. coloque no console o valor da posição zero do array acima

    console.log(student[0])
    
    //8. Crie um novo student e coloque na posição 1
    //Do array students

    const Jhon = {
        name: 'John',
        age: 30,
        weight: 88.6,
        isAdmin: true
    }

    students[1] = John
    console.log(students)

     /* 
        9. Sem rodar o código responda qual é a resposta
        do código abaixo e por que? Após sua resposta,
        rode o código e veja se voce acertou.

        console.log(a)
        var a = 1
        
     */